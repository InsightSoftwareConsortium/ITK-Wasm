Module['locateFile'] = function(fileName) {
  if(fileName === '@wasm_target@.wasm') {
    return '../ImageIOs/@wasm_target@.wasm'
  }
  return fileName
}

Module['runtimeInitializedResolve'] = function(resolve) {
  console.log('calledRun');
  console.log(calledRun);
  Module['onRuntimeInitialized'] = function() {
  console.log('calledRun');
  console.log(calledRun);
    console.log('RESOLVING onRuntimeInitialized');
    console.log(resolve);
    console.log(Module);
    resolve(Module);
  }
  console.log('runtimeInitialized value:');
  console.log(runtimeInitialized);
  console.log('calledRun');
  console.log(calledRun);
  if (runtimeInitialized) {
    resolve(Module);
  }
}
